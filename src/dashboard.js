import React, {useState} from 'react'
import {useNavigate} from 'react-router-dom'

const Dashboard = (props) => {

    return (
        <div className="mainContainer">
            I'm an unfinished dashboard!
        </div>
    )
}

export default Dashboard